const styles = {
  container: `
    min-h-[calc(100vh-var(--nav-height))]
    flex
    justify-center
    xs:items-center
    xs:py-8
    xs:px-4
    lg:mx-auto
    lg:w-1/2
  `,
};

export default styles;
