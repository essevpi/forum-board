const styles = {
  container: `
    flex
    justify-center
    w-full
    lg:w-3/4
    2xl:w-1/2
    lg:mx-auto
    min-h-[calc(100vh-var(--nav-height))]    
    sm:items-center
    sm:py-8
    sm:px-4
  `,
};

export default styles;
