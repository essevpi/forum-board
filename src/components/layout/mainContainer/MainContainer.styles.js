const styles = {
  container: `
    min-h-[calc(100vh-var(--nav-height))]    
    w-full
    flex
    justify-center
    lg:mx-auto
    lg:w-1/2
    sm:items-center
    sm:py-8
    sm:px-4
  `,
};

export default styles;
